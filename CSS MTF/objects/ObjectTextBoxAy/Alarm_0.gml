/// @description Inserte aquí la descripción
// Puede escribir su código en este editor
if(textOnDispley != text[page]){
    textOnDispley += string_char_at(text[page],string_length(textOnDispley)+1);
    alarm[0] = 1;
}
